import React from 'react'
import styled from 'styled-components'

export default function Title({ children }) {
  return <h2 className='text-blue-200 text-center font-bold text-4xl'>{children}</h2>
}
